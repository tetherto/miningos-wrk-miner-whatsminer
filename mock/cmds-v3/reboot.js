'use strict'

/**
 * V3 API reboot command handler
 *
 * Returns null to simulate miner rebooting (no response)
 */
module.exports = function (ctx, state) {
  // Simulate reboot - return null (no response, connection closed)
  return null
}
