'use strict'

/**
 * V3 API set.system.reboot command handler
 * Reboots the miner
 */
module.exports = function (ctx, state) {
  // Return null to simulate no response (miner is rebooting)
  return null
}
